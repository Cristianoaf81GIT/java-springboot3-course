spring:
  
  application:
      name: java-springboot3-course

  config:
    import: env.properties

  datasource:
    driver-class-name: ${DRIVER}
    url: ${URL} 
    username: ${USER_NAME} 
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
        #    properties:
        #hibernate:
        #dialect: 
    show-sql: false
    open-in-view: true

